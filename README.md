# Lawrence's Version Controlled Resume

- Can compile the [latex file](./main.tex) from command line using `pdflatex` or on [ overleaf ](www.overleaf.com)
- As of 2025-04-22 the [word doc](./resume.docx) is outdated
```bash
sudo apt install texlive-latex-extra
# Verify installation
pdflatex --version
```

